{% extends 'base.html.twig' %}

{% block body %}
    <h1>Login </h1>
    <div>
        <form method="post" action="{{ path('login_to_app') }}" >
            <div>
                <span>username</span>
                <input type="text" name="username"/>
            </div>
            <div>
                <span>password</span>
                <input type="password" name="password"/>
            </div>
            <input type="submit"> </form>
        </form>

    </div>
{% endblock %}
