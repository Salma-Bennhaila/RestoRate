{% extends 'base.html.twig' %}

{% block body %}
    <h1> la liste des restaurants créés </h1>
    <div>
        <table>
            <tr>
                <th>id Rest</th>
                <th>Nom Rest</th>
                <th>Date Creation</th>
                <th>Date Modification</th>
                <th>Ville </th>
                <th>Utilisateur</th>
                <th>Action</th>
            </tr>
            {% for restaurant in restaurants %}
                <tr>
                    <td>{{ restaurant.restaurantId }}</td>
                    <td>{{ restaurant.restaurantName }}</td>
                    <td>{{ restaurant.createAt.date|date("m/d/Y") }}</td>
                    <td> {{ restaurant.updateAt.date|date("m/d/Y") }}</td>
                    <td>{{ restaurant.cityId.cityName }}</td>
                    <td> {{ restaurant.userId.username }}</td>
                    <td>
                        <a href="{{ path('delete_restau',{id: restaurant.restaurantId}) }}"> delete </a>
                    </td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}